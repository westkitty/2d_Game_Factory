{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:sw2d:schema:content-players:v1",
  "title": "PlayerRosterDocument",
  "description": "Mirrors @sw2d/contracts PlayerRosterDocument (playerInput.ts). Content document name 'players'. Authoring this document is what opts a generated game into the input.players local-multiplayer routing capability; omitting it leaves the game on the single global ActionInput.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "minPlayers", "maxPlayers"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1, "maximum": 1 },
    "minPlayers": { "type": "integer", "minimum": 1 },
    "maxPlayers": { "type": "integer", "minimum": 1 },
    "requireReady": { "type": "boolean" },
    "playerIds": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Optional explicit slot ids. Must be unique and exactly maxPlayers long."
    },
    "deadzone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stick", "trigger"],
      "properties": {
        "stick": { "type": "number", "minimum": 0, "exclusiveMaximum": 1 },
        "trigger": { "type": "number", "minimum": 0, "exclusiveMaximum": 1 }
      }
    }
  }
}
