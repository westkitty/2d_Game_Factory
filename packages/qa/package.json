{
  "name": "@sw2d/qa",
  "version": "0.1.0",
  "private": true,
  "description": "Real-browser smoke harness: launches the system-installed Chrome via playwright-core (no bundled browser download), serves a production build locally, drives it, and asserts a terminal oracle. Dev-only; never shipped in a game build.",
  "type": "module",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "sideEffects": false,
  "devDependencies": {
    "playwright-core": "1.62.1"
  }
}
