{
  "name": "@sw2d/presets",
  "version": "0.1.0",
  "private": true,
  "description": "The genre preset catalogue: composition recipes over reusable controllers and system packs. Production code depends on contracts + packs' side-effect-free ids subpath only - no Ajv, no Phaser (see ADR-0015).",
  "type": "module",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "sideEffects": false,
  "dependencies": {
    "@sw2d/contracts": "0.1.0",
    "@sw2d/packs": "0.1.0"
  },
  "devDependencies": {
    "@sw2d/runtime": "0.1.0",
    "@sw2d/schemas": "0.1.0"
  }
}
