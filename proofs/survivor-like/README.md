# Proof Survivor Like

Secondary defining proof consumer for capability program Phase 13 (Run Lifecycle & Roguelite Meta-Progression).

Demonstrates:
- Arena encounter waves via `sw2d.encounters`.
- Wave-clearing and survival loop via `sw2d.runs`.
- Transient upgrade economy during runs.
- Persistent meta-progression across runs via `sw2d.progression`.
- Player combat state via `sw2d.combat`.
